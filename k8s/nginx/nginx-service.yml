apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: treeapi-project
  labels:
    app: treeapi
spec:
  type: NodePort
  selector:
    app: treeapi-nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30009